% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_learners.R
\name{RLearner}
\alias{RLearner}
\title{R-Learner: minimize R-loss with cross-fitting. Aligned with causalml BaseRLearner.
With \code{treatment_type = "continuous"}, uses the Robinson partial-linear nuisance
\eqn{m(X)=\mathbb{E}[Y\mid X]}, \eqn{e(X)=\mathbb{E}[D\mid X]} and weighted effect
regression on \eqn{(Y-m)/(D-e)} (cf. \code{\link{r_learner_continuous}} for dose grids).}
\usage{
RLearner(
  learner = "ranger",
  control_name = 0,
  n_fold = 5,
  ate_alpha = 0.05,
  treatment_type = c("binary", "continuous")
)
}
\arguments{
\item{ate_alpha}{confidence level for ATE interval (default 0.05)}

\item{treatment_type}{\code{"binary"} or \code{"continuous"}}
}
\value{
Object returned by \code{RLearner}.
}
\description{
R-Learner: minimize R-loss with cross-fitting. Aligned with causalml BaseRLearner.
With \code{treatment_type = "continuous"}, uses the Robinson partial-linear nuisance
\eqn{m(X)=\mathbb{E}[Y\mid X]}, \eqn{e(X)=\mathbb{E}[D\mid X]} and weighted effect
regression on \eqn{(Y-m)/(D-e)} (cf. \code{\link{r_learner_continuous}} for dose grids).
}
\examples{
\dontrun{
# Basic usage
# RLearner(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
