% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DMLearner.R
\name{doubleml_plr}
\alias{doubleml_plr}
\title{Fit Partially Linear DML via DoubleML (R) when available}
\usage{
doubleml_plr(
  X,
  treatment,
  y,
  ml_l = NULL,
  ml_m = NULL,
  ml_g = NULL,
  n_folds = 5L,
  n_rep = 1L,
  score = "partialling out",
  apply_cross_fitting = TRUE,
  dml_procedure = "dml2",
  return_ml_object = FALSE
)
}
\arguments{
\item{X}{covariate matrix or data.frame}

\item{treatment}{treatment vector (binary 0/1 uses \code{classif} learner by default; continuous \eqn{D} uses \code{regr})}

\item{y}{outcome vector}

\item{ml_l}{mlr3 learner for \eqn{E[Y|X]} (default: \code{lrn("regr.ranger")})}

\item{ml_m}{mlr3 learner for \eqn{E[D|X]} (default: \code{classif.ranger} if \code{treatment} is binary, else \code{regr.ranger})}

\item{ml_g}{mlr3 learner for \eqn{g_0(X) = E[Y - D\theta_0|X]}; required for \code{score = "IV-type"} or a custom score function (default: clone of \code{ml_l} when needed)}

\item{n_folds, n_rep}{cross-fitting folds and sample-splitting repetitions (passed to \code{DoubleMLPLR})}

\item{score}{\code{"partialling out"}, \code{"IV-type"}, or a custom score \code{function(y, d, l_hat, m_hat, g_hat, smpls)} returning \code{list(psi_a, psi_b)}}

\item{apply_cross_fitting}{logical; if \code{FALSE}, no cross-fitting (see DoubleML docs)}

\item{dml_procedure}{\code{"dml1"} or \code{"dml2"}}

\item{return_ml_object}{if \code{TRUE}, return the fitted \code{DoubleMLPLR} object as \code{dml_obj}}
}
\value{
List with \code{ate}, \code{ate_se}, \code{cate} (if predict available), and optionally \code{dml_obj}. Or NULL if DoubleML not installed.
}
\description{
If the \code{DoubleML} package is installed, fits a partially linear regression
model using \code{DoubleML::DoubleMLPLR} and returns ATE and optional CATE predictions.
Otherwise returns NULL. Useful for comparison with \code{LinearDML} or for using
DoubleML's inference (e.g. \code{confint}, \code{bootstrap}). Requires \code{mlr3} and
\code{mlr3learners} (dependencies of DoubleML). For a pre-built \code{DoubleMLData} object
and arbitrary learners (including pipelines), use \code{\link{doubleml_plr_fit_data}}.
}
\examples{
\dontrun{
# Basic usage
# doubleml_plr(...)
}
}
\references{
DoubleML: https://docs.doubleml.org ; Bach et al. (2021) arXiv:2103.09603
}
\seealso{
\code{\link{RCausalML-package}}
}
