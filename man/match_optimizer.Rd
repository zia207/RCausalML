% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{match_optimizer}
\alias{match_optimizer}
\title{Match optimizer: search for best caliper / pihat / score_cols}
\usage{
match_optimizer(
  df,
  treatment_col = "is_treatment",
  ps_col = "pihat",
  user_col = NULL,
  matching_covariates = "pihat",
  max_smd = 0.1,
  max_deviation = 0.1,
  caliper_range = c(0.01, 0.5),
  max_pihat_range = c(0.95, 0.999),
  max_iter_per_param = 5L,
  min_users_per_group = 1000L,
  smd_cols = "pihat",
  dev_cols_transformations = NULL,
  dev_factor = 1,
  verbose = TRUE
)
}
\arguments{
\item{df}{data.frame with treatment, propensity, and covariate columns}

\item{treatment_col}{name of treatment column (default "is_treatment")}

\item{ps_col}{name of propensity score column (default "pihat")}

\item{user_col}{optional column for counting unique users per group (default NULL)}

\item{matching_covariates}{character vector; covariates to assess balance (default "pihat")}

\item{max_smd}{maximum acceptable absolute SMD (default 0.1)}

\item{max_deviation}{maximum acceptable deviation for key variables (default 0.1)}

\item{caliper_range}{numeric vector or length-2 range for caliper search (default c(0.01, 0.5))}

\item{max_pihat_range}{numeric vector or length-2 range for max propensity filter (default c(0.95, 0.999))}

\item{max_iter_per_param}{number of values to try per parameter (default 5)}

\item{min_users_per_group}{minimum units per group in matched set (default 1000)}

\item{smd_cols}{character vector; covariates that contribute extra to score when SMD > max_smd (default "pihat")}

\item{dev_cols_transformations}{named list of functions (e.g. mean) for deviation (default pihat = mean)}

\item{dev_factor}{weight for deviation in score (default 1)}

\item{verbose}{logical; print progress (default TRUE)}
}
\value{
data.frame; best matched sample
}
\description{
Finds parameters that minimize a balance score: number of features with
SMD above \code{max_smd} plus deviation penalty for key variables.
Searches over max propensity threshold, which covariates to include in
matching, and caliper.
}
\examples{
\dontrun{
# Basic usage
# match_optimizer(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
