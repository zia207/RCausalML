% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tempoCausalVAE.R
\name{TemporalCausalVAE}
\alias{TemporalCausalVAE}
\title{Temporal Causal VAE (Variational Autoencoder with Causal Dynamics)}
\usage{
TemporalCausalVAE(
  obs_dim = 3,
  latent_dim = 3,
  hidden_dim = 128,
  beta = 4,
  gamma = 1,
  lambda_sparsity = 0.1
)
}
\arguments{
\item{obs_dim}{Observation dimension (sequence feature size).}

\item{latent_dim}{Latent dimension (and adjacency size).}

\item{hidden_dim}{Hidden size for GRU and MLP layers.}

\item{beta}{KL weight in the loss.}

\item{gamma}{Weight for DAG + sparsity penalty.}

\item{lambda_sparsity}{Weight for L1 sparsity on adjacency.}
}
\value{
An \code{nn_module} (R6) that can be trained with \code{temporal_causal_loss}.
}
\description{
A VAE for temporal sequences that learns a latent causal transition model.
Encoder (GRU) maps observed sequence to latent; dynamics evolve latent with
a learned adjacency; decoder (GRU) reconstructs from the latent trajectory.
Includes NOTEARS-style DAG penalty and sparsity on the adjacency.
}
\examples{
\dontrun{
# Basic usage
# function_name(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
