% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{causal_egm}
\alias{causal_egm}
\title{CausalEGM}
\usage{
causal_egm(
  X,
  treatment,
  y,
  dim_c = 8L,
  dim_t = 4L,
  dim_y = 4L,
  hidden_dim = 128L,
  num_epochs = 150L,
  batch_size = 256L,
  learning_rate = 0.001,
  learning_rate_disc = 5e-04,
  weight_decay = 1e-04,
  lambda_recon = 1,
  lambda_treat = 2,
  lambda_outcome = 2,
  lambda_disent = 0.5,
  max_grad_norm = 1,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{treatment}{Binary treatment vector (0/1).}

\item{y}{Outcome vector.}

\item{dim_c}{Latent size for confounding signal (default 8).}

\item{dim_t}{Latent size for treatment-specific signal (default 4).}

\item{dim_y}{Latent size for outcome-specific signal (default 4).}

\item{hidden_dim}{Hidden layer width (default 128).}

\item{num_epochs}{Number of training epochs (default 150).}

\item{batch_size}{Mini-batch size (default 256).}

\item{learning_rate}{Model learning rate (default 1e-3).}

\item{learning_rate_disc}{Discriminator learning rate (default 5e-4).}

\item{weight_decay}{Weight decay for model optimizer (default 1e-4).}

\item{lambda_recon}{Reconstruction loss weight (default 1).}

\item{lambda_treat}{Treatment loss weight (default 2).}

\item{lambda_outcome}{Outcome loss weight (default 2).}

\item{lambda_disent}{Disentanglement loss weight (default 0.5).}

\item{max_grad_norm}{Gradient clipping threshold (default 1).}

\item{verbose}{If TRUE, print progress every 10 epochs.}

\item{device}{Device string: \code{"cuda"}, \code{"cpu"}, or \code{NULL}.}

\item{...}{Ignored.}
}
\value{
Object of class \code{causal_egm}.
}
\description{
Fits a Causal Encoding Generative Model with disentangled latent factors for
confounding (\code{Zc}), treatment-specific (\code{Zt}), and outcome-specific
(\code{Zy}) signals. The model jointly optimizes reconstruction, treatment,
and outcome losses with a discriminator-based disentanglement term.
}
\details{
Ported from \code{causalEGM.ipynb} into package-safe R/\pkg{torch}.
}
\examples{
\dontrun{
# Basic usage
# causal_egm(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
