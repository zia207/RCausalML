% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize.R
\name{counterfactual_unit_selection}
\alias{counterfactual_unit_selection}
\title{Counterfactual Unit Selection: select units to maximize
beta*P(complier|X) + gamma*P(always-taker|X) + theta*P(never-taker|X) + delta*P(defier|X).
Requires estimates of P(favourable|X,W=0) and P(favourable|X,W=1) (e.g. from outcome model).
Complier: favourable iff treated. Always: favourable either way. Never: never favourable. Defier: favourable iff not treated.}
\usage{
counterfactual_unit_selection(
  X,
  y_fit_control,
  y_fit_treated,
  beta = 1,
  gamma = 0,
  theta = 0,
  delta = 0
)
}
\arguments{
\item{X}{covariates (matrix or data.frame)}

\item{y_fit_control}{predicted P(favourable | X, W=0)}

\item{y_fit_treated}{predicted P(favourable | X, W=1)}

\item{beta}{benefit for complier (default 1)}

\item{gamma}{benefit for always-taker (default 0)}

\item{theta}{benefit for never-taker (default 0)}

\item{delta}{benefit for defier (default 0)}
}
\value{
vector of unit selection scores (higher = better to treat)
}
\description{
Counterfactual Unit Selection: select units to maximize
beta*P(complier|X) + gamma*P(always-taker|X) + theta*P(never-taker|X) + delta*P(defier|X).
Requires estimates of P(favourable|X,W=0) and P(favourable|X,W=1) (e.g. from outcome model).
Complier: favourable iff treated. Always: favourable either way. Never: never favourable. Defier: favourable iff not treated.
}
\examples{
\dontrun{
# Basic usage
# counterfactual_unit_selection(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
