% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_learners.R
\name{estimate_ate.DRLearner}
\alias{estimate_ate.DRLearner}
\title{Estimate Average Treatment Effect (ATE) for DR-Learner}
\usage{
\method{estimate_ate}{DRLearner}(
  obj,
  X,
  treatment,
  y,
  p = NULL,
  return_ci = TRUE,
  bootstrap_ci = FALSE,
  n_bootstraps = 1000L,
  bootstrap_size = 10000L,
  n_cores = 1L,
  seed = NULL,
  pretrain = FALSE,
  ...
)
}
\arguments{
\item{obj}{DRLearner object (fitted or not)}

\item{X}{covariate matrix}

\item{treatment}{treatment vector}

\item{y}{outcome vector}

\item{p}{optional propensity (vector or list by group)}

\item{return_ci}{if FALSE return only \code{ate} vector (default TRUE)}

\item{bootstrap_ci}{if TRUE use bootstrap for CI instead of analytical SE}

\item{n_bootstraps}{number of bootstrap iterations (default 1000)}

\item{bootstrap_size}{number of samples per bootstrap (default 10000)}

\item{n_cores}{number of cores for bootstrap CI (1 = sequential)}

\item{seed}{random seed for fit and bootstrap}

\item{pretrain}{if TRUE, assume model already fitted (use predict only)}

\item{...}{passed to \code{fit} or \code{fit_predict}}
}
\value{
list with \code{ate}, \code{ate_lb}, \code{ate_ub} (vectors, one per treatment group); if \code{return_ci=FALSE} only \code{ate}
}
\description{
Uses analytical standard error (Imbens & Wooldridge 2009) by default; optional bootstrap CI.
Reference: formula (7) in "Recent Developments in the Econometrics of Program Evaluation."
}
\examples{
\dontrun{
# Basic usage
# estimate_ate.DRLearner(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
