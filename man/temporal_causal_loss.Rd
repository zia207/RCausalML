% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tempoCausalVAE.R
\name{temporal_causal_loss}
\alias{temporal_causal_loss}
\title{Total loss for the Temporal Causal VAE}
\usage{
temporal_causal_loss(x, dec_mu, dec_logvar, enc_mu, enc_logvar, model)
}
\arguments{
\item{x}{Ground-truth sequence  [batch, T, obs_dim]}

\item{dec_mu}{Decoder mean           [batch, obs_dim]}

\item{dec_logvar}{Decoder log-variance   [batch, obs_dim]}

\item{enc_mu}{Encoder mean           [batch, latent_dim]}

\item{enc_logvar}{Encoder log-variance   [batch, latent_dim]}

\item{model}{TemporalCausalVAE instance (hyperparams + penalty methods)}
}
\value{
Scalar tensor — total loss (lower is better)
}
\description{
Combines four terms:
  1. Reconstruction loss — Gaussian log-likelihood at the *last* timestep
  2. KL loss             — KL( q(z|x) || N(0,1) ), analytical
  3. DAG loss            — NOTEARS acyclicity penalty
  4. Sparsity loss       — L1 on adjacency matrix entries
}
\details{
total = -recon + beta * KL + gamma * (DAG + lambda_sparsity * sparsity)
}
\examples{
\dontrun{
# Basic usage
# temporal_causal_loss(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
