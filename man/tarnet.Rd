% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{tarnet}
\alias{tarnet}
\title{TARNet — Treatment-Agnostic Representation Network}
\usage{
tarnet(
  X,
  treatment,
  y,
  rep_dim = 100L,
  hidden = c(200L, 200L, 100L),
  batch_size = 64L,
  val_split = 0.2,
  epochs = 100L,
  lr = 0.001,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{covariate matrix or data.frame}

\item{treatment}{binary treatment 0/1}

\item{y}{outcome vector}

\item{rep_dim}{deprecated; use \code{hidden} for layer sizes}

\item{hidden}{vector of hidden layer sizes (default \code{c(200, 200, 100)})}

\item{batch_size}{batch size for training (default 64)}

\item{val_split}{fraction for validation (default 0.2)}

\item{epochs}{number of training epochs (default 100)}

\item{lr}{learning rate (default 1e-3)}

\item{verbose}{if TRUE, print epoch loss}

\item{...}{ignored}
}
\value{
Object of class \code{tarnet} with \code{predict()} for ITE.
}
\description{
TARNet uses a shared representation \eqn{\Phi(X)} and two heads for \eqn{\hat{Y}(0)} and \eqn{\hat{Y}(1)}.
Architecture follows \href{https://github.com/arnaudscott/TARNet}{TARNet}: MLP representation (ELU), then treatment-specific outputs.
}
\details{
When \pkg{torch} is installed, the full TARNet is fitted. Otherwise falls back to an nnet or ranger placeholder.
}
\examples{
\dontrun{
# Basic usage
# tarnet(...)
}
}
\references{
\url{https://github.com/arnaudscott/TARNet}
}
\seealso{
\code{\link{RCausalML-package}}
}
