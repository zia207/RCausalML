% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_learners.R
\name{r_learner_continuous}
\alias{r_learner_continuous}
\title{Generalized R-learner for continuous treatment (dose-response on a grid)}
\usage{
r_learner_continuous(
  data,
  outcome = "Y",
  treatment = "D",
  covariates = NULL,
  dose_grid = seq(0, 6, by = 0.5),
  learner = c("ranger", "lm", "xgb"),
  n_fold = 5L,
  bandwidth = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} with outcome, treatment, and covariates.}

\item{outcome}{Name of the outcome column (default \code{"Y"}).}

\item{treatment}{Name of the continuous treatment column (default \code{"D"}).}

\item{covariates}{Character vector of covariate column names. If \code{NULL}, uses
\code{paste0("X", 1:10)} when those columns exist, otherwise all columns except
\code{outcome} and \code{treatment}.}

\item{dose_grid}{Dose values at which to estimate \eqn{\partial E[Y(d)\mid X]/\partial d}
(local linear slope in \eqn{D}).}

\item{learner}{\code{"ranger"}, \code{"lm"}, or \code{"xgb"} (same spirit as \code{\link{RLearner}}).}

\item{n_fold}{Number of cross-fitting folds for nuisance models.}

\item{bandwidth}{Kernel half-width for local regression; default \code{sd(D) * n^(-1/5)}.}

\item{seed}{Optional integer; if set, fold assignment is reproducible.}

\item{...}{Passed to \code{ranger::ranger} or \code{xgboost::xgboost}.}
}
\value{
A list with \code{dose_grid}, \code{tau_grid} (matrix, length(\code{dose_grid}) by \eqn{n}),
  \code{X} (covariate matrix), and cross-fitted \code{mu_hat}, \code{e_hat}.
}
\description{
Cross-fits nuisance regressions for \eqn{E[Y \mid D, X]} and \eqn{E[D \mid X]}, then
runs weighted local linear regressions of residuals on a \code{dose_grid} (Epanechnikov
weights). Base learners match \code{\link{RLearner}}: \code{"ranger"}, \code{"lm"}, or
\code{"xgb"}.
}
\examples{
\dontrun{
# Basic usage
# r_learner_continuous(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
