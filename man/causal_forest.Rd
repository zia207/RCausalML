% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalForest.R
\name{causal_forest}
\alias{causal_forest}
\title{Fit a Causal Forest}
\usage{
causal_forest(
  X,
  Y,
  W,
  Y.hat = NULL,
  W.hat = NULL,
  num.trees = 2000,
  sample.weights = NULL,
  clusters = NULL,
  equalize.cluster.weights = FALSE,
  sample.fraction = 0.5,
  mtry = min(ceiling(sqrt(ncol(X)) + 20), ncol(X)),
  min.node.size = 5,
  honesty = TRUE,
  honesty.fraction = 0.5,
  honesty.prune.leaves = TRUE,
  alpha = 0.05,
  imbalance.penalty = 0,
  stabilize.splits = TRUE,
  ci.group.size = 2,
  tune.parameters = "none",
  compute.oob.predictions = TRUE,
  num.threads = 1,
  seed = NULL
)
}
\arguments{
\item{X}{Numeric covariate matrix (n x p)}

\item{Y}{Numeric outcome vector (length n)}

\item{W}{Binary or continuous treatment vector (length n)}

\item{Y.hat}{Pre-estimated E[Y|X]; estimated if NULL}

\item{W.hat}{Pre-estimated E[W|X]; estimated if NULL}

\item{num.trees}{Number of trees}

\item{sample.weights}{Observation weights; uniform if NULL}

\item{clusters}{Cluster IDs for cluster-robust sampling (optional)}

\item{equalize.cluster.weights}{Balance sampling across clusters}

\item{sample.fraction}{Fraction of data per tree}

\item{mtry}{Features tried per split}

\item{min.node.size}{Minimum observations in a leaf}

\item{honesty}{Use honest splitting}

\item{honesty.fraction}{Fraction of subsample used for estimation}

\item{honesty.prune.leaves}{Remove leaves with no estimation samples}

\item{alpha}{Minimum split fraction (regularization)}

\item{imbalance.penalty}{Penalty on unbalanced splits}

\item{stabilize.splits}{Use W residuals in split criterion}

\item{ci.group.size}{Trees grouped for variance estimation}

\item{tune.parameters}{"none" or "all"}

\item{compute.oob.predictions}{Compute out-of-bag CATE predictions}

\item{num.threads}{Parallel threads (uses parallel package if > 1)}

\item{seed}{Random seed for reproducibility}
}
\value{
A causal_forest object
}
\description{
Fit a Causal Forest
}
\examples{
\dontrun{
# Basic usage
# causal_forest(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
