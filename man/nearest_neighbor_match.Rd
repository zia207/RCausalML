% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match.R
\name{nearest_neighbor_match}
\alias{nearest_neighbor_match}
\title{Nearest neighbor propensity score matching}
\usage{
nearest_neighbor_match(
  data,
  treatment_col = "w",
  score_cols = "e",
  caliper = 0.2,
  replace = FALSE,
  ratio = 1L,
  shuffle = TRUE,
  treatment_to_control = TRUE,
  random_state = NULL
)
}
\arguments{
\item{data}{data.frame containing treatment and score columns}

\item{treatment_col}{name of treatment column (0/1)}

\item{score_cols}{character vector; column name(s) for matching (e.g. propensity)}

\item{caliper}{threshold in SD units (or in scaled units when replace=TRUE) to accept a match (default 0.2)}

\item{replace}{logical; allow reuse of controls (default FALSE)}

\item{ratio}{integer; number of control units per treated (default 1)}

\item{shuffle}{logical; shuffle treatment group before matching (default TRUE)}

\item{treatment_to_control}{logical; if TRUE match treatment to control, else control to treatment (default TRUE)}

\item{random_state}{integer or NULL; seed for reproducibility}
}
\value{
data.frame with matched sample (treated + matched controls)
}
\description{
Matches on propensity (and optionally other columns). Supports caliper,
with/without replacement, ratio, shuffle, and matching treatment-to-control
or control-to-treatment. When \code{replace=TRUE}, multiple \code{score_cols}
are supported (with scaling); when \code{replace=FALSE}, only a single
score column is allowed.
}
\examples{
\dontrun{
# Basic usage
# nearest_neighbor_match(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
