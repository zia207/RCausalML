% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{ivae}
\alias{ivae}
\title{iVAE (Identifiable Variational Autoencoder)}
\usage{
ivae(
  X,
  u,
  latent_dim = 2L,
  hidden_dim = 128L,
  n_aux = NULL,
  dropout = 0.15,
  num_epochs = 100L,
  batch_size = 128L,
  learning_rate = 0.001,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{u}{Auxiliary variable (integer labels from 0 to \code{n_aux - 1}).}

\item{latent_dim}{Latent dimension (default 2).}

\item{hidden_dim}{Hidden dimension (default 128).}

\item{n_aux}{Number of auxiliary classes (default inferred from \code{u}).}

\item{dropout}{Dropout probability (default 0.15).}

\item{num_epochs}{Number of epochs (default 100).}

\item{batch_size}{Batch size (default 128).}

\item{learning_rate}{Learning rate (default 1e-3).}

\item{verbose}{If TRUE, prints training loss every 10 epochs.}

\item{device}{Device string: \code{"cuda"}, \code{"cpu"}, or \code{NULL}.}

\item{...}{Ignored.}
}
\value{
Object of class \code{ivae}.
}
\description{
Fits an iVAE model with auxiliary variable \code{u} and predicts latent representation \eqn{z}.
This is a package-safe integration of \code{causal_iVAE.R} without load-time side effects.
}
\examples{
\dontrun{
# Basic usage
# ivae(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
