% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R, R/synthetic_data.R
\name{synthetic_data}
\alias{synthetic_data}
\title{Main entry: generate synthetic data by mode}
\usage{
synthetic_data(mode = 2, n = 15000, p = 20, sigma = 5.5)

synthetic_data(mode = 2, n = 15000, p = 20, sigma = 5.5)
}
\arguments{
\item{mode}{Integer. \code{1} = difficult nuisance + easy treatment (see \code{\link{simulate_nuisance_and_easy_treatment}}); \code{2} = nonlinear baseline + strong heterogeneity (MVN covariates).}

\item{n}{Integer. Sample size}

\item{p}{Integer. Number of covariates (mode 1 requires \code{p >= 5}; mode 2 requires \code{p >= 11})}

\item{sigma}{Numeric. Noise standard deviation for outcome}

\item{adj}{propensity adjustment (modes 1,4)}
}
\value{
list with y, X, w, tau, b, e (or as separate vectors if requested)

A named list with:
  y: outcome vector (n)
  X: covariate matrix (n × p)
  w: treatment vector (0/1) (n)
  tau: true individual treatment effect (n)
  b: baseline function value E[Y| X, W=0] (n)
  e: propensity score P(W=1 | X) (n)
}
\description{
Main entry: generate synthetic data by mode

Generate synthetic data similar to causalml.dataset.synthetic_data(mode=2)
}
\examples{
\dontrun{
# Basic usage
# synthetic_data(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
