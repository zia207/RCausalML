% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{castle}
\alias{castle}
\title{CASTLE (CAusal STructure LEarning Regularization)}
\usage{
castle(
  X,
  y,
  hidden_dim = 64L,
  num_layers = 3L,
  lambda_reg = 1,
  beta_sparsity = 0.1,
  acyc_weight = 1,
  recon_weight = 0.5,
  neighbor_temp = 10,
  y_index = NULL,
  epochs = 200L,
  batch_size = 128L,
  learning_rate = 0.001,
  weight_decay = 1e-05,
  verbose = TRUE,
  device = NULL,
  threshold = 0.05,
  ...
)
}
\arguments{
\item{X}{Numeric matrix or data.frame of predictors.}

\item{y}{Numeric outcome.}

\item{hidden_dim}{Hidden layer width (default 64).}

\item{num_layers}{Number of predictor layers (default 3).}

\item{lambda_reg}{Weight on prediction loss (default 1).}

\item{beta_sparsity}{L1 weight on adjacency (default 0.1).}

\item{acyc_weight}{Acyclicity penalty weight (default 1).}

\item{recon_weight}{Neighborhood reconstruction weight (default 0.5).}

\item{neighbor_temp}{Temperature for neighborhood soft mask (default 10).}

\item{y_index}{1-based index of outcome column in \code{Z=[X,y]}. Default last column.}

\item{epochs}{Number of training epochs (default 200).}

\item{batch_size}{Batch size (default 128).}

\item{learning_rate}{Adam learning rate (default 1e-3).}

\item{weight_decay}{Adam weight decay (default 1e-5).}

\item{verbose}{If TRUE, print progress every 20 epochs.}

\item{device}{Device string: \code{"cuda"}, \code{"cpu"}, or \code{NULL}.}

\item{threshold}{Threshold used by summary/plot for edge filtering (default 0.05).}

\item{...}{Ignored.}
}
\value{
Object of class \code{castle}.
}
\description{
Torch implementation of CASTLE translated from the Python tutorial module:
learn weighted adjacency \eqn{A}, optimize prediction + sparsity + acyclicity
(NOTEARS-style) + neighborhood reconstruction regularization.
}
\examples{
\dontrun{
# Basic usage
# castle(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
