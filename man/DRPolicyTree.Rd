% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy_learner.R
\name{DRPolicyTree}
\alias{DRPolicyTree}
\title{DR Policy Tree (EconML-style)}
\usage{
DRPolicyTree(
  model_regression = "ranger",
  model_propensity = "glmnet",
  min_propensity = 1e-06,
  control_name = 0,
  cv = 2L,
  max_depth = NULL,
  min_samples_split = 10L,
  min_samples_leaf = 5L,
  max_features = "auto",
  random_state = NULL
)
}
\arguments{
\item{model_regression}{character: outcome regression learner ("ranger", "lm", "glmnet", "xgb"). Default \code{"ranger"}.}

\item{model_propensity}{character: propensity learner for optional DR weighting ("glmnet", "ranger", "glm"). Default \code{"glmnet"}.}

\item{min_propensity}{numeric: lower bound for propensity (default \code{1e-6}).}

\item{control_name}{value indicating control group (default \code{0}).}

\item{cv}{integer: number of cross-fitting folds (default \code{2}).}

\item{max_depth}{integer or NULL: maximum tree depth for the policy tree (default \code{NULL}).}

\item{min_samples_split}{integer: minimum samples to split a node (default \code{10}).}

\item{min_samples_leaf}{integer: minimum samples per leaf (default \code{5}).}

\item{max_features}{character or integer: "auto", "sqrt", "log2", or number (default \code{"auto"}).}

\item{random_state}{integer or NULL: random seed.}
}
\value{
Object of class \code{DRPolicyTree}.
}
\description{
Policy learner that uses cross-fitted outcome regression to form a value matrix
(predicted CATE per treatment vs control), then fits a single decision tree to predict
the best treatment (argmax value). Mirrors EconML \code{econml.policy.DRPolicyTree}.
}
\examples{
\dontrun{
# Basic usage
# DRPolicyTree(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
