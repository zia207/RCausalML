% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalBoosted_IV.R
\name{.crossfit_cxgboost}
\alias{.crossfit_cxgboost}
\title{Cross-fitted nuisance predictions via CXGBoost (parallelised)}
\usage{
.crossfit_cxgboost(
  X,
  outcome,
  treat,
  n_folds = 5L,
  nrounds = 100L,
  xgb_params = list()
)
}
\arguments{
\item{X}{Covariate matrix (n x p), no NAs.}

\item{outcome}{Numeric outcome vector (Y, W, or Z).}

\item{treat}{Binary 0/1 vector used as the CXGBoost treatment mask.}

\item{n_folds}{Number of folds (default 5).}

\item{nrounds}{XGBoost boosting rounds per fold (default 100).}

\item{xgb_params}{Extra XGBoost hyperparameters.}
}
\value{
List: \code{y0_hat}, \code{y1_hat}, \code{propensity} (all length n,
  out-of-fold).
}
\description{
Runs k-fold cross-fitting in parallel via \pkg{future.apply}.  Each fold
trains a fresh \code{CXGBoost} model on the complement and predicts on the
held-out fold.
}
\details{
When \code{outcome} is continuous (> 2 unique values) the function still
uses CXGBoost's two-head architecture; the "treatment mask" (\code{treat})
determines which head each observation updates  this is valid for
continuous outcomes because the mask is applied to the gradient, not to a
classification loss.
}
\keyword{internal}
