% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{cevae}
\alias{cevae}
\title{CEVAE (Counterfactual Variational Autoencoder)}
\usage{
cevae(
  X,
  treatment,
  y,
  outcome_dist = "normal",
  latent_dim = 20L,
  hidden_dim = 200L,
  num_epochs = 50L,
  num_layers = 3L,
  batch_size = 100L,
  learning_rate = 0.001,
  learning_rate_decay = 0.1,
  num_samples = 1000L,
  weight_decay = 1e-04,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{treatment}{Binary treatment 0/1.}

\item{y}{Outcome vector.}

\item{outcome_dist}{Outcome distribution: \code{"normal"} (default) or \code{"bernoulli"}.}

\item{latent_dim}{Dimension of latent confounder (default 20).}

\item{hidden_dim}{Hidden layer size (default 200).}

\item{num_epochs}{Number of training epochs (default 50).}

\item{num_layers}{Number of hidden layers (default 3).}

\item{batch_size}{Batch size (default 100).}

\item{learning_rate}{Learning rate (default 1e-3).}

\item{learning_rate_decay}{Final lr = lr * this (default 0.1).}

\item{num_samples}{Number of samples for ITE prediction (default 1000).}

\item{weight_decay}{Weight decay (default 1e-4).}

\item{verbose}{If TRUE, print epoch loss.}

\item{device}{Device for torch: \code{"cuda"}, \code{"cpu"}, or \code{NULL} (default: use CUDA if available, else CPU).}

\item{...}{Ignored.}
}
\value{
Object of class \code{cevae} with \code{predict()} for ITE.
}
\description{
Generative model: z ~ p(z), x ~ p(x|z), w ~ p(w|z), y ~ p(y|t,z). Twin outcome nets allow imbalanced treatment.
Training: ELBO + log q(t|x) + log q(y|t,x). When \pkg{torch} is available the full VAE is fitted; else nnet/ranger placeholder.
}
\examples{
\dontrun{
# Basic usage
# cevae(...)
}
}
\references{
Louizos et al. (2017). Causal Effect Inference with Deep Latent-Variable Models. NeurIPS.
}
\seealso{
\code{\link{RCausalML-package}}
}
