% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/automated_ml.R
\name{add_automated_ml}
\alias{add_automated_ml}
\alias{addAutomatedML}
\title{Replace EconAutoMLConfig args with AutomatedMLModel instances}
\usage{
add_automated_ml(args, workspace = NULL)

addAutomatedML(args, workspace = NULL)
}
\arguments{
\item{args}{named list of arguments (e.g. \code{list(learner = my_config)}).
Any element that is an \code{EconAutoMLConfig} is replaced by an
\code{AutomatedMLModel} with that config.}

\item{workspace}{optional; passed to \code{AutomatedMLModel} (unused in local port).}
}
\value{
List with same names; configs replaced by \code{AutomatedMLModel} objects.

Object returned by \code{function_name}.
}
\description{
In Python, \code{addAutomatedML} is a mixin that wraps the base estimator so
any \code{EconAutoMLConfig} in constructor args is turned into an
\code{AutomatedMLModel}. In R we provide a helper that takes a list of
arguments (e.g. for a meta-learner) and replaces any \code{EconAutoMLConfig}
with an \code{AutomatedMLModel} so you can pass the result into estimators
that accept "learner" or model objects.
}
\examples{
\dontrun{
# Basic usage
# add_automated_ml(...)
}
\dontrun{
# Basic usage
# function_name(...)
}
}
\references{
EconML \code{addAutomatedML}.
}
\seealso{
\code{\link{RCausalML-package}}

\code{\link{RCausalML-package}}
}
