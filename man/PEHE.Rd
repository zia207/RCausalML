% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalBoosted_IV.R, R/causalXGBoost.R
\name{PEHE}
\alias{PEHE}
\title{Precision in Estimation of Heterogeneous Effects (PEHE)}
\usage{
PEHE(y = NULL, y_hat = NULL, tau_true = NULL, tau_hat = NULL)

PEHE(y = NULL, y_hat = NULL, tau_true = NULL, tau_hat = NULL)
}
\arguments{
\item{y}{n x 2 matrix \code{[Y(0), Y(1)]}. Ignored when
\code{tau_true} is supplied.}

\item{y_hat}{n x 2 matrix \code{[Yhat(0), Yhat(1)]}. Ignored when
\code{tau_hat} is supplied.}

\item{tau_true}{Numeric vector of true ITEs (vector form).}

\item{tau_hat}{Numeric vector of estimated ITEs (vector form).}
}
\value{
Numeric scalar (RMSE on the ITE scale).

Numeric scalar.
}
\description{
Root mean squared error between true and estimated individual treatment
effects: \eqn{\sqrt{E[(\tau(x) - \hat\tau(x))^2]}}.

Accepts two calling conventions:
\enumerate{
  \item \strong{Matrix form} (original): \code{PEHE(y, y_hat)} where both
    arguments are n x 2 matrices with columns \code{[Y(0), Y(1)]}.
    Returns MSE of the ITE: \eqn{E[((Y(1)-Y(0)) - (\hat Y(1)-\hat Y(0)))^2]}.
  \item \strong{Vector form} (new): \code{PEHE(tau_true = ..., tau_hat = ...)}
    where both arguments are length-n numeric vectors of individual treatment
    effects. Returns RMSE: \eqn{\sqrt{E[(\tau - \hat\tau)^2]}}.
}
}
\examples{
PEHE(tau_true = c(1, 2, 3), tau_hat = c(1.1, 1.9, 3.2))
# Matrix form
y     <- matrix(c(1, 2, 0.5, 1.5), ncol = 2)
y_hat <- matrix(c(1.1, 2.2, 0.4, 1.6), ncol = 2)
PEHE(y, y_hat)

# Vector form
PEHE(tau_true = c(1, 2, 3), tau_hat = c(1.1, 1.9, 3.2))
}
\seealso{
\code{\link{RCausalML-package}}

\code{\link{RCausalML-package}}
}
