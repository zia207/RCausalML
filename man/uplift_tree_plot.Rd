% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uplift_trees.R
\name{uplift_tree_plot}
\alias{uplift_tree_plot}
\title{Plot an uplift tree (base R), with arrows connecting each split to its children}
\usage{
uplift_tree_plot(
  tree,
  x_names = NULL,
  main = "Uplift tree",
  use_rpart_plot = FALSE,
  arrow_length = 0.08,
  color_leaves_by_tau = FALSE,
  ...
)
}
\arguments{
\item{tree}{tree list or fitted object (uplift_tree_ddp, etc.)}

\item{x_names}{character vector of feature names (optional)}

\item{main}{optional title}

\item{use_rpart_plot}{if \code{TRUE}, attempt to plot via \pkg{rpart.plot} (requires
converting the tree to \code{rpart} format; needs \pkg{rpart} and \pkg{rpart.plot}).
If \code{FALSE} (default), use the built-in plot with arrows.}

\item{arrow_length}{size of arrowhead in user coordinates (default 0.08); larger values give bigger arrowheads.}

\item{color_leaves_by_tau}{if \code{TRUE}, color leaf nodes by uplift (tau): blue for higher,
green for lower (similar to Python causalml \code{plot.py} upliftScoreToColor).}

\item{...}{passed to \code{plot()} or \code{rpart.plot()}}
}
\value{
Invisible node layout data (when \code{use_rpart_plot = FALSE}) or the return
  value of \code{rpart.plot} when \code{use_rpart_plot = TRUE}.
}
\description{
Edges are drawn with arrowheads pointing to child nodes (similar to Python CausalML's
graphviz-style uplift_tree_plot). Optionally use \code{rpart.plot} when available
(see \code{use_rpart_plot}) for a polished rpart-style diagram.
}
\examples{
\dontrun{
# Basic usage
# uplift_tree_plot(...)
}
}
\seealso{
\code{\link{uplift_tree_string}}, \code{\link{uplift_rf_kl}}, \code{\link{uplift_forest_plot}}.
  Python reference: \url{https://github.com/uber/causalml/blob/master/causalml/inference/tree/plot.py}
}
