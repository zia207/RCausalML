% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{propensity_model_gradient_boosted}
\alias{propensity_model_gradient_boosted}
\title{Gradient boosted propensity model (XGBoost)}
\usage{
propensity_model_gradient_boosted(
  clip_bounds = c(0.001, 1 - 0.001),
  calibrate = TRUE,
  early_stop = FALSE,
  stop_val_size = 0.2,
  max_depth = 8L,
  learning_rate = 0.1,
  n_estimators = 100L,
  colsample_bytree = 0.8,
  random_state = 42L,
  ...
)
}
\arguments{
\item{clip_bounds}{length-2 numeric: bounds for clipping.}

\item{calibrate}{logical: whether to calibrate (default \code{TRUE}).}

\item{early_stop}{logical: whether to use a validation set for early stopping (default \code{FALSE}).}

\item{stop_val_size}{numeric: fraction of data for validation when \code{early_stop=TRUE} (default 0.2).}

\item{max_depth, learning_rate, n_estimators, colsample_bytree, random_state}{passed to xgboost; defaults mirror Python.}

\item{...}{other arguments passed to \code{xgboost::xgb.train} or \code{xgboost::xgb.Booster}.}
}
\value{
Propensity model object with \code{fit}, \code{predict}, \code{fit_predict}.
}
\description{
Propensity model using \code{xgboost::xgb.train} / \code{xgboost::xgb.DMatrix} or
\code{xgboost::xgb.Booster} for binary classification. Optional early stopping.
Requires the \code{xgboost} package. Mirrors Python \code{GradientBoostedPropensityModel}.
}
\examples{
\dontrun{
# Basic usage
# propensity_model_gradient_boosted(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
