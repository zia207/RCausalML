% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_learners.R
\name{SLearner}
\alias{SLearner}
\title{S-Learner: single model with treatment as feature
CATE(x) = mu(x,1) - mu(x,0). Aligned with causalml BaseSLearner.
With \code{treatment_type = "continuous"}, fits one surface \eqn{\mu(X, D)} and
predicts \eqn{\mu(X, d_1) - \mu(X, d_0)} using \code{dose_values} (default: 25th and
75th percentiles of training \eqn{D}).}
\usage{
SLearner(
  learner = "ranger",
  control_name = 0,
  ate_alpha = 0.05,
  treatment_type = c("binary", "continuous"),
  dose_values = NULL
)
}
\arguments{
\item{learner}{character "lm", "glmnet", "ranger", or "xgb"; or a list with fit/predict functions}

\item{control_name}{value for control group (0 or "control"); ignored when \code{treatment_type = "continuous"}}

\item{ate_alpha}{confidence level for ATE interval (default 0.05)}

\item{treatment_type}{\code{"binary"} (default) or \code{"continuous"}}

\item{dose_values}{length-2 numeric \eqn{(d_0, d_1)} for continuous contrast; if \code{NULL}, set at fit from training quantiles 0.25 and 0.75}
}
\value{
Object returned by \code{SLearner}.
}
\description{
S-Learner: single model with treatment as feature
CATE(x) = mu(x,1) - mu(x,0). Aligned with causalml BaseSLearner.
With \code{treatment_type = "continuous"}, fits one surface \eqn{\mu(X, D)} and
predicts \eqn{\mu(X, d_1) - \mu(X, d_0)} using \code{dose_values} (default: 25th and
75th percentiles of training \eqn{D}).
}
\examples{
\dontrun{
# Basic usage
# SLearner(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
