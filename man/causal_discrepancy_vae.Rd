% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{causal_discrepancy_vae}
\alias{causal_discrepancy_vae}
\title{CausalDiscrepancyVAE}
\usage{
causal_discrepancy_vae(
  X,
  treatment,
  y,
  latent_dim = 16L,
  hidden_dim = 128L,
  beta = 1,
  lambda_mmd = 1,
  lambda_y = 10,
  n_kernels = 5L,
  num_epochs = 100L,
  batch_size = 128L,
  learning_rate = 0.001,
  weight_decay = 1e-04,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{treatment}{Binary treatment vector (0/1).}

\item{y}{Outcome vector.}

\item{latent_dim}{Latent dimension (default 16).}

\item{hidden_dim}{Hidden dimension (default 128).}

\item{beta}{KL weight (default 1).}

\item{lambda_mmd}{MMD penalty weight (default 1).}

\item{lambda_y}{Outcome loss weight (default 10).}

\item{n_kernels}{Number of RBF kernels for MMD (default 5).}

\item{num_epochs}{Number of training epochs (default 100).}

\item{batch_size}{Batch size (default 128).}

\item{learning_rate}{Learning rate (default 1e-3).}

\item{weight_decay}{Weight decay for AdamW (default 1e-4).}

\item{verbose}{If TRUE, print progress every 10 epochs.}

\item{device}{Device string: \code{"cuda"}, \code{"cpu"}, or \code{NULL}.}

\item{...}{Ignored.}
}
\value{
Object of class \code{causal_discrepancy_vae}.
}
\description{
Fits a discrepancy-regularized causal VAE with latent MMD balancing between
treatment groups and treatment-specific outcome heads.
Ported from \code{causalDiscrepanct_VAE.ipynb} into package-safe R/\pkg{torch}.
}
\examples{
\dontrun{
# Basic usage
# causal_discrepancy_vae(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
