% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{dragonnet}
\alias{dragonnet}
\title{DragonNet — shared representation and three heads (torch)}
\usage{
dragonnet(
  X,
  treatment,
  y,
  neurons = 200L,
  reg_l2 = 0.01,
  targeted_reg = TRUE,
  ratio_tar = 1,
  batch_size = 64L,
  val_split = 0.2,
  adam_epochs = 30L,
  adam_lr = 0.001,
  sgd_epochs = 100L,
  sgd_lr = 1e-05,
  sgd_momentum = 0.9,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{covariate matrix or data.frame}

\item{treatment}{binary treatment 0/1}

\item{y}{outcome vector}

\item{neurons}{units per shared layer (default 200)}

\item{reg_l2}{L2 regularization for outcome heads (default 0.01)}

\item{targeted_reg}{if TRUE (default), use targeted regularization in the loss}

\item{ratio_tar}{weight for targeted regularization term (default 1)}

\item{batch_size}{batch size for training (default 64)}

\item{val_split}{fraction for validation (default 0.2)}

\item{adam_epochs}{epochs for Adam phase (default 30)}

\item{adam_lr}{Adam learning rate (default 1e-3)}

\item{sgd_epochs}{epochs for SGD phase (default 100)}

\item{sgd_lr}{SGD learning rate (default 1e-5)}

\item{sgd_momentum}{SGD momentum (default 0.9)}

\item{verbose}{if TRUE, print epoch loss}

\item{...}{ignored}
}
\value{
Object of class \code{dragonnet} (torch model when available, else placeholder) with \code{predict()} for ITE and optional \code{propensity}.
}
\description{
DragonNet uses a shared representation and three heads: propensity \eqn{\hat{e}(X)}, \eqn{\hat{Y}(0)}, and \eqn{\hat{Y}(1)},
with optional targeted regularization (learnable \eqn{\varepsilon}). Architecture follows
[causalml dragonnet.py](https://github.com/uber/causalml/blob/master/causalml/inference/tf/dragonnet.py):
3 shared layers (ELU), propensity head (sigmoid), and two outcome branches (2 hidden layers each, then output).
}
\details{
When \pkg{torch} is installed, the full DragonNet is fitted (Adam then SGD). Otherwise falls back to an nnet or ranger placeholder.
}
\examples{
\dontrun{
# Basic usage
# dragonnet(...)
}
}
\references{
Shi, C., Blei, D., Veitch, V. (2019). Adapting Neural Networks for the Estimation of Treatment Effects. \url{https://arxiv.org/abs/1906.02120}.

\url{https://github.com/uber/causalml/blob/master/causalml/inference/tf/dragonnet.py}
}
\seealso{
\code{\link{RCausalML-package}}
}
