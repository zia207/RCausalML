% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cate_interpreter.R
\name{SingleTreePolicyInterpreter}
\alias{SingleTreePolicyInterpreter}
\title{Single-tree policy interpreter}
\usage{
SingleTreePolicyInterpreter(
  risk_level = NULL,
  risk_seeking = FALSE,
  max_depth = NULL,
  min_samples_split = 2L,
  min_samples_leaf = 1L,
  min_weight_fraction_leaf = 0,
  max_features = NULL,
  min_balancedness_tol = 0.45,
  min_impurity_decrease = 0,
  random_state = NULL
)
}
\arguments{
\item{risk_level}{numeric or NULL. If NULL, point CATE is used; if set (e.g.
0.05), the lower (risk_seeking=FALSE) or upper (risk_seeking=TRUE) end of
a confidence interval can be used (requires estimator to support intervals).}

\item{risk_seeking}{logical (default FALSE). If \code{risk_level} is set,
use upper (TRUE) or lower (FALSE) end of the interval as effect.}

\item{max_depth}{integer or NULL. Maximum depth of the policy tree.}

\item{min_samples_split}{integer (default 2).}

\item{min_samples_leaf}{integer (default 1).}

\item{min_weight_fraction_leaf}{numeric (default 0).}

\item{max_features}{integer, character, or NULL.}

\item{min_balancedness_tol}{numeric in [0, 0.5] (default 0.45). Minimum
fraction of samples that must fall on each side of a split.}

\item{min_impurity_decrease}{numeric (default 0).}

\item{random_state}{integer or NULL.}
}
\value{
Object of class \code{SingleTreePolicyInterpreter}.
}
\description{
Interprets a treatment assignment policy based on a CATE estimator by
fitting a single decision tree that predicts the best treatment (control
vs treat) from covariates. Aligns with EconML
\code{econml.cate_interpreter.SingleTreePolicyInterpreter}.
}
\examples{
\dontrun{
# Basic usage
# SingleTreePolicyInterpreter(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
