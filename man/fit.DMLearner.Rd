% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DMLearner.R
\name{fit.DMLearner}
\alias{fit.DMLearner}
\title{Fit DMLearner — cross-fitting and final stage (EconML \code{DML.fit})}
\usage{
\method{fit}{DMLearner}(obj, X, treatment, y, p = NULL, ...)
}
\arguments{
\item{X}{covariate matrix or data frame (\eqn{n \times d_x}); concatenate controls \eqn{W} manually if needed.}

\item{treatment}{vector, or \eqn{n \times d} matrix / data frame of continuous treatments (\eqn{d > 1}).}

\item{y}{outcome vector (\eqn{n}).}

\item{p}{optional propensity \eqn{E[T\mid X]} override (binary / discrete scalar \eqn{T} only).}

\item{\dots}{passed to first-stage learners (\code{ranger}, \code{xgboost}, etc.).}
}
\value{
Object returned by \code{fit.DMLearner}.
}
\description{
Cross-validated nuisance fits per fold (analogous to EconML \code{_OrthoLearner} / \code{cv}), then
stacks out-of-fold residuals for the final CATE regression. Optional \code{p} fixes \eqn{E[T\mid X]}
for binary treatment (skips cross-fitted \eqn{\hat{f}}).
}
\examples{
\dontrun{
# Basic usage
# fit.DMLearner(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
