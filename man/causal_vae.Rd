% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{causal_vae}
\alias{causal_vae}
\title{CausalVAE}
\usage{
causal_vae(
  X,
  latent_dim = 3L,
  hidden_dim = 64L,
  beta = 1,
  gamma = 0.01,
  lambda_sparsity = 0.001,
  alpha = 0.001,
  num_epochs = 200L,
  batch_size = 256L,
  learning_rate = 1e-04,
  weight_decay = 1e-05,
  warmup_epochs = 40L,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{latent_dim}{Latent dimension (default 3).}

\item{hidden_dim}{Hidden dimension (default 64).}

\item{beta}{KL weight (default 1).}

\item{gamma}{Causal penalty weight (default 0.01).}

\item{lambda_sparsity}{Sparsity weight (default 0.001).}

\item{alpha}{DAG penalty scale (default 0.001).}

\item{num_epochs}{Number of epochs (default 200).}

\item{batch_size}{Batch size (default 256).}

\item{learning_rate}{Learning rate (default 1e-4).}

\item{weight_decay}{Weight decay (default 1e-5).}

\item{warmup_epochs}{Warmup epochs for causal term scaling (default 40).}

\item{verbose}{If TRUE, print progress.}

\item{device}{Device string: \code{"cuda"}, \code{"cpu"}, or \code{NULL}.}

\item{...}{Ignored.}
}
\value{
Object of class \code{causal_vae}.
}
\description{
Fits a causal variational autoencoder with DAG and sparsity penalties.
This integrates the package-safe model from \code{causalVAE.R}.
}
\examples{
\dontrun{
# Basic usage
# causal_vae(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
