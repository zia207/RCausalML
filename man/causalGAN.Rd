% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{causalGAN}
\alias{causalGAN}
\title{CausalGAN (Structural Equation GAN)}
\usage{
causalGAN(
  X,
  treatment,
  y,
  hidden_dim = 128L,
  noise_x = NULL,
  noise_t = 4L,
  noise_y = 4L,
  epochs = 300L,
  batch_size = 128L,
  lr_g = 2e-04,
  lr_d = 2e-04,
  lambda_label = 0.5,
  label_smooth = 0.9,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{treatment}{Binary treatment 0/1.}

\item{y}{Outcome vector.}

\item{hidden_dim}{Hidden dimension for all MLPs (default 128).}

\item{noise_x}{Noise dimension for \eqn{G_X} (default ncol(X)).}

\item{noise_t}{Noise dimension for \eqn{G_T} (default 4).}

\item{noise_y}{Noise dimension for \eqn{G_Y} (default 4).}

\item{epochs}{Number of training epochs (default 300).}

\item{batch_size}{Batch size (default 128).}

\item{lr_g}{Learning rate for generator (default 2e-4).}

\item{lr_d}{Learning rate for discriminator/labellers (default 2e-4).}

\item{lambda_label}{Weight of labeller loss relative to GAN loss (default 0.5).}

\item{label_smooth}{Smoothed real label for BCE (default 0.9).}

\item{verbose}{If TRUE, print progress every 50 epochs.}

\item{device}{Device for torch: \code{"cuda"}, \code{"cpu"}, or \code{NULL} (default: CUDA if available, else CPU).}

\item{...}{Ignored.}
}
\value{
Object of class \code{causalGAN}. Use \code{predict()} for interventional samples and ITE.
}
\description{
CausalGAN models \eqn{X \rightarrow T \rightarrow Y} with node-wise generators:
\eqn{G_X(\epsilon_X)}, \eqn{G_T(X, \epsilon_T)}, \eqn{G_Y(X, T, \epsilon_Y)}.
A global discriminator scores the full joint \eqn{(X, T, Y)} while auxiliary labellers
regularize each causal component to improve structural equation fidelity and
interventional calibration.
}
\examples{
\dontrun{
# Basic usage
# causalGAN(...)
}
}
\references{
CausalGAN notebook in this package (\code{causal_gan.ipynb}).
}
\seealso{
\code{\link{RCausalML-package}}
}
