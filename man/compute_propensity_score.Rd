% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{compute_propensity_score}
\alias{compute_propensity_score}
\title{Compute propensity scores (train and optionally predict)}
\usage{
compute_propensity_score(
  X,
  treatment,
  p_model = NULL,
  X_pred = NULL,
  treatment_pred = NULL,
  calibrate_p = TRUE,
  clip_bounds = c(0.001, 1 - 0.001)
)
}
\arguments{
\item{X}{matrix or data.frame: features for training.}

\item{treatment}{numeric or factor: binary treatment vector (0/1) for training.}

\item{p_model}{optional: a propensity model object with \code{fit()} and \code{predict()};
or any model with \code{predict(..., type="response")} or \code{predict_proba}.
If \code{NULL}, an elastic net propensity model is created and fitted.}

\item{X_pred}{matrix or data.frame: features for prediction; if \code{NULL}, uses \code{X}.}

\item{treatment_pred}{optional: treatment vector for calibration target; if \code{NULL}, uses \code{treatment}.}

\item{calibrate_p}{logical: whether to calibrate propensity scores (default \code{TRUE});
only used when \code{p_model} is \code{NULL} (the default model is created with \code{calibrate=calibrate_p}).}

\item{clip_bounds}{length-2 numeric: bounds for clipping (default \code{c(1e-3, 1 - 1e-3)}).}
}
\value{
List with \code{p}: numeric vector of propensity scores, and \code{p_model}: the fitted
  propensity model (either the provided one or the created elastic net model).
}
\description{
Fits a propensity model on \code{(X, treatment)} and returns propensity scores
for \code{X_pred} (or \code{X}). If \code{p_model} is not provided, uses
\code{propensity_model_elastic_net}. Mirrors Python \code{compute_propensity_score}.
}
\examples{
\dontrun{
# Basic usage
# compute_propensity_score(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
