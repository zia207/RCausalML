% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_learners.R
\name{estimate_ate}
\alias{estimate_ate}
\title{Estimate Average Treatment Effect from a fitted meta-learner or fit-predict}
\usage{
estimate_ate(
  obj,
  X,
  treatment,
  y,
  p = NULL,
  return_ci = TRUE,
  bootstrap_ci = FALSE,
  n_bootstraps = 1000L,
  bootstrap_size = 10000L,
  n_cores = 1L,
  pretrain = FALSE,
  learner = "ranger",
  ...
)
}
\arguments{
\item{obj}{meta-learner object (SLearner, TLearner, etc.) or character "S","T","X","R","DR"}

\item{X}{covariates}

\item{treatment}{treatment vector}

\item{y}{outcome}

\item{p}{propensity (optional, for X/R/DR)}

\item{return_ci}{return confidence interval}

\item{bootstrap_ci}{use bootstrap for CI instead of analytical SE}

\item{n_cores}{number of cores for bootstrap CI (1 = sequential)}

\item{pretrain}{if TRUE assume model already fitted}

\item{learner}{base learner for meta-learner if obj is character}
}
\value{
ate (and ate_lb, ate_ub if return_ci). For DRLearner see \code{estimate_ate.DRLearner}.
}
\description{
Estimate Average Treatment Effect from a fitted meta-learner or fit-predict
}
\examples{
\dontrun{
# Basic usage
# estimate_ate(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
