% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalTree.R
\name{causal_tree}
\alias{causal_tree}
\title{Causal Tree (Honest or Adaptive)}
\usage{
causal_tree(
  formula,
  data,
  treatment,
  honest = TRUE,
  est_fraction = 0.5,
  est_idx = NULL,
  split.Rule = "CT",
  cv.option = "CT",
  minsize = 5L,
  propensity = NULL,
  split.alpha = 0.5,
  cv.alpha = 0.5,
  split.Bucket = FALSE,
  bucketNum = 5L,
  bucketMax = 100L,
  xval = 5L,
  cp = 0,
  ...
)
}
\arguments{
\item{formula}{Formula: response ~ predictors (no interactions)}

\item{data}{Data frame containing the variables in `formula`}

\item{treatment}{Binary vector (0 = control, 1 = treated), same length as rows in `data`}

\item{honest}{Logical. If `TRUE` (default), uses honest estimation:
tree structure built on training sample, leaf means estimated on held-out estimation sample.}

\item{est_fraction}{Fraction of data used for honest estimation (when `honest = TRUE`).
Ignored if `est_idx` is provided. Default: 0.5}

\item{est_idx}{Optional integer vector: row indices to use for honest estimation.
If provided, overrides random split and `est_fraction`.}

\item{split.Rule}{Splitting rule: `"TOT"`, `"CT"`, `"fit"`, `"tstats"` (or with `"D"` suffix for bucketed)}

\item{cv.option}{Cross-validation method: `"none"`, `"TOT"`, `"matching"`, `"CT"`, `"fit"`, `"user"`, `"policy"`}

\item{minsize}{Minimum number of treated **and** control units per leaf. Default: 5L}

\item{propensity}{Scalar propensity score (constant). Defaults to observed mean(treatment)}

\item{split.alpha}{Parameter for honest splitting risk function (01). Default: 0.5}

\item{cv.alpha}{Parameter for CV risk function (01). Default: 0.5}

\item{split.Bucket}{Logical. Use bucketed (discrete) splitting?}

\item{bucketNum, bucketMax}{Bucket parameters (when `split.Bucket = TRUE`)}

\item{xval}{Number of cross-validation folds. Default: 5}

\item{cp}{Complexity parameter (can be 0 for no pruning during growth)}

\item{...}{Passed to `rpart.control()` (e.g. `minsplit`, `maxdepth`, `xval`)}
}
\value{
Object of class `c("causalTree", "rpart")`
  - Use `rpart.plot::rpart.plot()` to visualize
  - Use `prune()` to prune via cross-validation error
  - Use `predict.causalTree()` (from previous response) for predictions
}
\description{
Fits a causal tree for heterogeneous treatment effects using recursive partitioning.
Supports both **adaptive** (original `causalTree` style) and **honest** estimation
(recommended; Athey & Imbens 2016) in one function.
}
\examples{
\dontrun{
  # Honest causal tree (recommended)
  ct_honest <- causal_tree(
    y ~ x1 + x2 + x3 + x4,
    data = mydata,
    treatment = mydata$trt,
    honest = TRUE,
    est_fraction = 0.4,
    split.Rule = "CT",
    cv.option = "CT",
    minsize = 10L,
    xval = 5
  )

  # Prune using CV error
  cp_best <- ct_honest$cptable[which.min(ct_honest$cptable[,"xerror"]), "CP"]
  pruned <- prune(ct_honest, cp = cp_best)

  rpart.plot::rpart.plot(pruned, roundint = FALSE)

  # Predict CATE on new data
  tau_new <- predict(pruned, newdata = testdata, type = "treatment.effect")
}

}
\references{
Athey, S., & Imbens, G. (2016). Recursive partitioning for heterogeneous causal effects.
Proceedings of the National Academy of Sciences.

Original implementation: <https://github.com/susanathey/causalTree>
Maintained fork: **htetree** on CRAN
}
\seealso{
\code{\link{RCausalML-package}}
}
