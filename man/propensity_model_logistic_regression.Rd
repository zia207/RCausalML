% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{propensity_model_logistic_regression}
\alias{propensity_model_logistic_regression}
\title{Logistic regression propensity model (elastic net)}
\usage{
propensity_model_logistic_regression(
  clip_bounds = c(0.001, 1 - 0.001),
  calibrate = TRUE,
  n_fold = 4L,
  alpha = 0.5,
  ...
)
}
\arguments{
\item{clip_bounds}{length-2 numeric: bounds for clipping (default \code{c(1e-3, 1 - 1e-3)}).}

\item{calibrate}{logical: whether to calibrate with isotonic regression (default \code{TRUE}).}

\item{n_fold}{integer: number of CV folds for lambda selection (default 4).}

\item{alpha}{numeric: elastic net mixing (0=ridge, 1=lasso; default 0.5).}

\item{...}{passed to \code{glmnet::cv.glmnet}.}
}
\value{
Propensity model object with \code{fit}, \code{predict}, \code{fit_predict}.
}
\description{
Propensity model using \code{glmnet::cv.glmnet} with binomial family and elastic net
(alpha in (0,1)). Mirrors Python \code{LogisticRegressionPropensityModel}.
}
\examples{
\dontrun{
# Basic usage
# propensity_model_logistic_regression(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
