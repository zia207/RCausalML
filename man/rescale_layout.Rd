% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawing.R
\name{rescale_layout}
\alias{rescale_layout}
\title{Rescale layout positions to (-scale, scale) on all axes}
\usage{
rescale_layout(pos, scale = 1)
}
\arguments{
\item{pos}{Numeric matrix; each row is a position (e.g. n x 2 for 2D).}

\item{scale}{Scale factor; extent is (-scale, scale) in each axis.}
}
\value{
Matrix of rescaled positions (same dimensions as \code{pos}).
}
\description{
Centers positions by subtracting the mean per axis, then scales so the
largest absolute coordinate equals \code{scale}. Preserves aspect ratio.
}
\examples{
\dontrun{
# Basic usage
# rescale_layout(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
