% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{ganite}
\alias{ganite}
\title{GANITE — Causal Inference with Generative Adversarial Nets}
\usage{
ganite(
  X,
  treatment,
  y,
  h_dim = 50L,
  iterations = 1000L,
  batch_size = 64L,
  alpha = 1,
  beta = 1,
  lr = 1e-04,
  dropout = FALSE,
  use_adamw = FALSE,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{treatment}{Binary treatment 0/1.}

\item{y}{Outcome vector.}

\item{h_dim}{Hidden layer size for Generator, Discriminator, and Inference net (default 50).}

\item{iterations}{Number of training iterations (default 1000).}

\item{batch_size}{Batch size (default 64).}

\item{alpha}{Weight for GAN loss in Generator (default 1).}

\item{beta}{Weight for ATE-supervised loss in Inference net (default 1).}

\item{lr}{Learning rate for all nets (default 1e-4).}

\item{dropout}{If TRUE, use dropout 0.2 in all nets (default FALSE).}

\item{use_adamw}{If TRUE, use AdamW optimizer (default FALSE).}

\item{verbose}{If TRUE, print progress every 100 iterations.}

\item{device}{Device for torch: \code{"cuda"}, \code{"cpu"}, or \code{NULL} (default: use CUDA if available, else CPU).}

\item{...}{Ignored.}
}
\value{
Object of class \code{ganite} with \code{predict()} for ITE. When \pkg{torch} is not available, falls back to nnet or ranger placeholder.
}
\description{
GANITE uses a Generator to produce counterfactual outcomes, a Discriminator to encourage
balanced representations, and an Inference network that maps covariates to potential outcomes
\eqn{\hat{Y}(0)}, \eqn{\hat{Y}(1)} for ITE. Requires \pkg{torch}.
}
\examples{
\dontrun{
# Basic usage
# ganite(...)
}
}
\references{
Yoon J., Jordon J., van der Schaar M. (2018). GANITE: Estimation of Individualized Treatment Effects using Generative Adversarial Nets. ICLR. \url{https://arxiv.org/abs/1809.00916}
}
\seealso{
\code{\link{RCausalML-package}}
}
