% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy_learner.R
\name{DRPolicyForest}
\alias{DRPolicyForest}
\title{DR Policy Forest (EconML-style)}
\usage{
DRPolicyForest(
  model_regression = "ranger",
  model_propensity = "glmnet",
  min_propensity = 1e-06,
  control_name = 0,
  cv = 2L,
  n_estimators = 100L,
  max_samples = 0.5,
  max_depth = NULL,
  min_samples_split = 10L,
  min_samples_leaf = 5L,
  max_features = "auto",
  random_state = NULL,
  n_jobs = 1L
)
}
\arguments{
\item{model_regression}{character: outcome regression learner (default \code{"ranger"}).}

\item{model_propensity}{character: propensity learner (default \code{"glmnet"}).}

\item{min_propensity}{numeric: lower bound for propensity (default \code{1e-6}).}

\item{control_name}{value indicating control group (default \code{0}).}

\item{cv}{integer: number of cross-fitting folds (default \code{2}).}

\item{n_estimators}{integer: number of trees in the forest (default \code{100}).}

\item{max_samples}{numeric in (0,1]: fraction of samples per tree (default \code{0.5}).}

\item{max_depth}{integer or NULL: maximum tree depth (default \code{NULL}).}

\item{min_samples_split}{integer: minimum samples to split (default \code{10}).}

\item{min_samples_leaf}{integer: minimum samples per leaf (default \code{5}).}

\item{max_features}{character or integer (default \code{"auto"}).}

\item{random_state}{integer or NULL: random seed.}

\item{n_jobs}{integer: number of parallel jobs (default \code{1}; uses \code{parallel} if > 1).}
}
\value{
Object of class \code{DRPolicyForest}.
}
\description{
Ensemble of policy trees fit on subsamples; prediction by majority vote.
Mirrors EconML \code{econml.policy.DRPolicyForest}.
}
\examples{
\dontrun{
# Basic usage
# DRPolicyForest(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
