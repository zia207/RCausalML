% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{cfrnet}
\alias{cfrnet}
\title{CFRNet — Counterfactual Regression Network (TARNet + MMD balancing)}
\usage{
cfrnet(
  X,
  treatment,
  y,
  rep_dim = 100L,
  hidden = c(200L, 200L, 100L),
  mmd_weight = 0.1,
  sigma_mmd = 1,
  batch_size = 64L,
  val_split = 0.2,
  epochs = 100L,
  lr = 0.001,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{covariate matrix or data.frame}

\item{treatment}{binary treatment 0/1}

\item{y}{outcome vector}

\item{rep_dim}{deprecated; use \code{hidden} for layer sizes}

\item{hidden}{vector of hidden layer sizes (default \code{c(200, 200, 100)})}

\item{mmd_weight}{weight for MMD balancing term (default 0.1)}

\item{sigma_mmd}{RBF kernel bandwidth for MMD (default 1)}

\item{batch_size}{batch size for training (default 64)}

\item{val_split}{fraction for validation (default 0.2)}

\item{epochs}{number of training epochs (default 100)}

\item{lr}{learning rate (default 1e-3)}

\item{verbose}{if TRUE, print epoch loss}

\item{...}{ignored}
}
\value{
Object of class \code{cfrnet} with \code{predict()} for ITE.
}
\description{
CFRNet adds explicit balancing via MMD^2 (RBF kernel) on the representation \eqn{\Phi(X)} between treated and control,
as in \href{https://github.com/clinicalml/cfrnet}{cfrnet}. Same architecture as TARNet (shared representation + two heads).
}
\details{
When \pkg{torch} is installed, the full CFRNet is fitted. Otherwise falls back to an nnet or ranger placeholder.
}
\examples{
\dontrun{
# Basic usage
# cfrnet(...)
}
}
\references{
\url{https://github.com/clinicalml/cfrnet}
}
\seealso{
\code{\link{RCausalML-package}}
}
