% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causalDeepNet.R
\name{dscm}
\alias{dscm}
\title{Deep Structural Causal Model (DSCM)}
\usage{
dscm(
  X,
  treatment,
  y,
  y0_true = NULL,
  y1_true = NULL,
  hidden_dim = 256L,
  treatment_hidden_dim = 128L,
  latent_dim = 16L,
  dropout = 0.1,
  num_epochs = 300L,
  batch_size = 256L,
  learning_rate = 2e-04,
  weight_decay = 1e-05,
  val_frac = 0.15,
  kl_warmup_epochs = 80L,
  max_beta = 0.25,
  lambda_oracle = 0,
  patience = 60L,
  max_grad_norm = 5,
  verbose = TRUE,
  device = NULL,
  ...
)
}
\arguments{
\item{X}{Covariate matrix or data.frame.}

\item{treatment}{Binary treatment vector (0/1).}

\item{y}{Outcome vector.}

\item{y0_true}{Optional true \eqn{Y(0)} for oracle supervision (e.g., IHDP simulations).}

\item{y1_true}{Optional true \eqn{Y(1)} for oracle supervision (e.g., IHDP simulations).}

\item{hidden_dim}{Hidden size for outcome network (default 256).}

\item{treatment_hidden_dim}{Hidden size for treatment network (default 128).}

\item{latent_dim}{Latent noise dimension for abduction (default 16).}

\item{dropout}{Dropout rate (default 0.1).}

\item{num_epochs}{Number of epochs (default 300).}

\item{batch_size}{Batch size (default 256).}

\item{learning_rate}{AdamW learning rate (default 2e-4).}

\item{weight_decay}{AdamW weight decay (default 1e-5).}

\item{val_frac}{Validation fraction (default 0.15).}

\item{kl_warmup_epochs}{Number of KL warmup epochs (default 80).}

\item{max_beta}{Maximum KL weight after warmup (default 0.25).}

\item{lambda_oracle}{Oracle supervision weight (default 0).}

\item{patience}{Early stopping patience on validation objective (default 60).}

\item{max_grad_norm}{Gradient clipping norm (default 5).}

\item{verbose}{If TRUE, print progress every 25 epochs.}

\item{device}{Device string: \code{"cuda"}, \code{"cpu"}, or \code{NULL}.}

\item{...}{Ignored.}
}
\value{
Object of class \code{dscm}.
}
\description{
Fits a Deep Structural Causal Model following the tutorial in
\code{deep_structural _causalML_DSCMs.ipynb}. The model contains:
(1) a treatment mechanism \eqn{p(T \mid X)} and
(2) an outcome mechanism with latent abduction \eqn{q(Z \mid X, T, Y)}
and potential-outcome heads \eqn{Y(0), Y(1)}.
}
\details{
Counterfactual inference follows abduction-action-prediction (AAP):
infer latent noise from factual data, intervene on treatment, then
decode counterfactual outcomes.
}
\examples{
\dontrun{
# Basic usage
# dscm(...)
}
}
\seealso{
\code{\link{RCausalML-package}}
}
